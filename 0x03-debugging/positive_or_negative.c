#include <stdlib.h>
#include <time.h>
/**
 *  * main - prints sign of number
 *   *
 *    * Return: 0 if no error
 */
int main(void)
{
	int n;

	srand(time(0));
	n = rand() - RAND_MAX / 2;

	return (0);
}
